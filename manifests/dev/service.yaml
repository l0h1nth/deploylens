apiVersion: v1
kind: Service
metadata:
  name: deploylens-sample
  namespace: deploylens-dev
  labels:
    app.kubernetes.io/name: deploylens-sample
    app.kubernetes.io/environment: dev
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: deploylens-sample
    app.kubernetes.io/environment: dev
  ports:
    - name: http
      port: 80
      targetPort: http
